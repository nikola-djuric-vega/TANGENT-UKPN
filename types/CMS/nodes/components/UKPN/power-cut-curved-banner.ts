import { ComponentsTypeName } from '_types/CMS'
import { IconNames } from '_types/local'

export interface PowerCutCurvedBannerType {
  icon?: IconNames
  tagLine?: string
  title?: string
  subTitle?: string
  status?: string
  backButton?: boolean
  __typename: ComponentsTypeName.POWER_CUT_CURVED_BANNER
}
